# --- Python & Django Internals ---
__pycache__/
*.py[cod]
*$py.class
*.log
*.pot

# --- Virtual Environments (VITAL) ---
venv/
env/
myenv/
.env
.venv/

# --- Local Database ---
# We ignore SQLite because production will likely use PostgreSQL.
# Even if using SQLite in prod, you don't want to overwrite live data with local dev data.
db.sqlite3
db.sqlite3-journal

# --- Static & Media Files ---
# Ignore uploaded user content (product images, invoices)
media/
# Exception: If you want to keep the folder structure but not files, you can use:
# media/*
# !media/.gitkeep

# Ignore collected static files (generated by python manage.py collectstatic)
# Keep your source 'static/' folder, but ignore the production output folder.
staticfiles/

# --- OS & IDE Metadata (Clutter) ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# --- Email & Secret Settings ---
# If you eventually split settings into local.py / prod.py
**/settings/local.py